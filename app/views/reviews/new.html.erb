<h2>Create a new review:</h2>

<%= simple_form_for [ @restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, selected: 1 %>
  <%= f.submit "Create new review", class: "btn btn-primary" %>
<% end %>
